Inclua o token Bearer em todas as requisições:

```bash
Authorization: Bearer hm_live_xxxxxxxxxxxx
```

Tokens são gerados em **Configurações → Desenvolvedores** no painel. Cada token tem escopos que limitam quais operações ele pode executar.
